doctype
html(lang="en", itemtype="http://schema.org/WebPage" itemscope)
  
  head(prefix="og: http://ogp.me/ns#")
    include _meta.jade
    include _css.jade
  
  body
    
    h1 
      img(src="/react-logo.png" alt="React.js")
      | React.careers

    section
      h2 Find the jobs working favourite framework: React.js

    section
      h2 Companies already using React.js:
      
      .button.red Add a company

      each data, title in companies
        .company
          img(src=data.logo alt=title)

    include _scripts.jade
